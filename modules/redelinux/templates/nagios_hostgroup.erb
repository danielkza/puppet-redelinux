define hostgroup {
    hostgroup_name    <%= @hostgroup_name %>
    alias             <%= @hostgroup_alias %>
<% if @hostgroup_members && !@hostgroup_members.empty? -%>
    hostgroup_members <%= @hostgroup_members.join(',') %>
<% end -%>
}

