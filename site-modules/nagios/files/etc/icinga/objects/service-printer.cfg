define service {
    use                 generic-service
    hostgroup_name      printers
    service_description check-printer
    check_command       check_printer
    check_interval      10
    retry_interval      1
}
